
const { expect } = require("expect");
const searchEmployee = require("./bonus");
const bonus = require("./bonus");

describe("searchEmployee", () => {
  it("É uma função?", () => {
    expect(typeof searchEmployee).toBe("function");
  });
  it("ID não existe retorne ID não identificada", () => {
    expect()
  })
});
